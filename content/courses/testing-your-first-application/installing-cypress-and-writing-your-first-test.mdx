# Installing Cypress and writing your first test

<YouTube id="x0QuiEJUf6s" title="Testing your first application - Lesson 02 - Installing Cypress and writing your first test"/>

## Installing Cypress

Before we can write any tests, we need to add Cypress to our project. From your terminal, run:

```bash
npm install cypress --save-dev
```

With Cypress installed, launch it with:

```bash
npx cypress open
```

![Choose testing type](/images/testing-your-first-application/installing-cypress-and-writing-your-first-test/choose_testing_type.jpg)

:::tip
You can click on the images to enlarge them.
:::

Cypress can run two kinds of tests: end-to-end (E2E) tests and component tests. In this course we will focus exclusively on E2E tests.

Click the “E2E Testing” button on the left to get started.

![Configuration files](/images/testing-your-first-application/installing-cypress-and-writing-your-first-test/configuration_files.jpg)

Cypress will show you the configuration files it is about to create to set up E2E testing. Click the “Continue” button at the bottom.

![Choose a browser](/images/testing-your-first-application/installing-cypress-and-writing-your-first-test/choose_a_browser.jpg)

Next you will see the “Choose a Browser” screen. The options you see depend on which browsers are installed on your computer. For this course we will run all of our tests in Chrome.

:::info
If you do not see “Chrome” as an option, it isn't installed. You can download it [here](https://www.google.com/chrome/), then re-launch Cypress.
:::

Click “Chrome,” then click the “Start E2E Testing in Chrome” button.

![Create your first spec](/images/testing-your-first-application/installing-cypress-and-writing-your-first-test/create_your_first_spec.jpg)

Since we don't have any test files yet, Cypress prompts us to create our first spec.

Click the “Create new spec” button.

![Create spec](/images/testing-your-first-application/installing-cypress-and-writing-your-first-test/create_spec.jpg)

We are going to test the application's home page, so rename this file to “home.cy.ts”.

![Screen Shot 2022-06-28 at 9.03.26 AM.png](/images/testing-your-first-application/installing-cypress-and-writing-your-first-test/Screen_Shot_2022-06-28_at_9.03.26_AM.png)

Then click “Create Spec.”

![Screen Shot 2022-06-28 at 9.03.44 AM.png](/images/testing-your-first-application/installing-cypress-and-writing-your-first-test/Screen_Shot_2022-06-28_at_9.03.44_AM.png)

Then click the “Okay, run the spec” button.

![Screen Shot 2022-06-28 at 9.03.51 AM.png](/images/testing-your-first-application/installing-cypress-and-writing-your-first-test/Screen_Shot_2022-06-28_at_9.03.51_AM.png)

Cypress runs the new spec against its [Kitchen Sink](https://example.cypress.io/) application.

Now that the spec file exists, let's update it to test our course application instead.

## Breaking down the home spec file

Open the `cypress/e2e/home.cy.ts` file in VSCode.

![Screen Shot 2022-06-28 at 9.06.55 AM.png](/images/testing-your-first-application/installing-cypress-and-writing-your-first-test/Screen_Shot_2022-06-28_at_9.06.55_AM.png)

This is the default test Cypress generated for us. We will rewrite it to test our course application, but first let's walk through the code that is already here.

```ts
describe("empty spec", () => {
  it("passes", () => {
    cy.visit("https://example.cypress.io")
  })
})
```

The first line is what's commonly called a “describe block.”

```ts
describe("empty spec", () => {})
```

The `describe()` function takes two arguments: a string describing the tests it contains, and a [callback function](https://developer.mozilla.org/en-US/docs/Glossary/Callback_function). Since this file will hold tests for our home page, let's change the description to “home page.”

```ts
describe("home page", () => {
  it("passes", () => {
    cy.visit("https://example.cypress.io")
  })
})
```

Inside the `describe()` body is an “it block.”

This is our actual test.

Every `it()` in a spec file represents a single test. It takes the same arguments as `describe()`: a string followed by a callback function. Let's update its description:

```ts
describe("home page", () => {
  it("the h1 contains the correct text", () => {
    cy.visit("https://example.cypress.io")
  })
})
```

Our first test will assert that the `h1` on the home page contains the correct text (highlighted in the screenshot below).

![Screen Shot 2022-06-28 at 9.15.48 AM.png](/images/testing-your-first-application/installing-cypress-and-writing-your-first-test/Screen_Shot_2022-06-28_at_9.15.48_AM.png)

Finally, inside the `it()` body we have [cy.visit()](https://docs.cypress.io/api/commands/visit).

[visit](https://docs.cypress.io/api/commands/visit) tells Cypress which URL to load before running the test. We will run our application locally at [localhost:3000](https://docs.cypress.io/api/commands/visit), so update `cy.visit()` with that address.

```ts
describe("home page", () => {
  it("the h1 contains the correct text", () => {
    cy.visit("http://localhost:3000")
  })
})
```

:::info
Some screenshots in this lesson may show `https://localhost:3000`, but the course app runs over plain `http`. Use `http://localhost:3000` (without the **s**) so your test matches the app.
:::

Now let's run the test.

:::info
If Cypress is still open from earlier in this lesson, keep using that same window—there's no need to launch it again. If you closed it, relaunch Cypress from your terminal:

```bash
npx cypress open
```

Cypress can only control one browser at a time, so avoid running more than one instance at once. If a previous run is still open, close it before starting a new one.
:::

![Screen Shot 2022-06-28 at 9.21.56 AM.png](/images/testing-your-first-application/installing-cypress-and-writing-your-first-test/Screen_Shot_2022-06-28_at_9.21.56_AM.png)

Click “E2E Testing.”

![Screen Shot 2022-06-28 at 9.22.20 AM.png](/images/testing-your-first-application/installing-cypress-and-writing-your-first-test/Screen_Shot_2022-06-28_at_9.22.20_AM.png)

Then click “Chrome,” followed by the “Start E2E Testing in Chrome” button.

![Screen Shot 2022-06-28 at 9.22.30 AM.png](/images/testing-your-first-application/installing-cypress-and-writing-your-first-test/Screen_Shot_2022-06-28_at_9.22.30_AM.png)

Next, click the “home.cy.ts” spec file.

![Screen Shot 2022-06-28 at 9.23.00 AM.png](/images/testing-your-first-application/installing-cypress-and-writing-your-first-test/Screen_Shot_2022-06-28_at_9.23.00_AM.png)

You should see the following.

![Screen Shot 2022-06-28 at 9.23.24 AM.png](/images/testing-your-first-application/installing-cypress-and-writing-your-first-test/Screen_Shot_2022-06-28_at_9.23.24_AM.png)

## Debugging our first error

If your application's dev server is **not** running, Cypress can't load **localhost:3000** and throws an error. Cypress always needs your app's local dev server running while your tests execute—otherwise you'll see this error.

:::info
If the dev server is still running from the previous lesson (you started it with `npm run dev`), your test is already passing and you won't see this error—you can skim this section and continue. We're walking through it anyway because a missing dev server is one of the most common errors you'll run into, and it's worth understanding why it happens.
:::

Cypress is currently running in our terminal:

![Screen Shot 2022-06-28 at 9.25.52 AM.png](/images/testing-your-first-application/installing-cypress-and-writing-your-first-test/Screen_Shot_2022-06-28_at_9.25.52_AM.png)

So we need a second terminal window to run the dev server. In VSCode, open one via Terminal > New Terminal in the menu bar.

![Screen Shot 2022-06-28 at 9.26.20 AM.png](/images/testing-your-first-application/installing-cypress-and-writing-your-first-test/Screen_Shot_2022-06-28_at_9.26.20_AM.png)

This opens a new terminal inside VSCode along with a small sidebar on the right. You can switch between terminals by clicking the icons in that sidebar.

![Screen Shot 2022-06-28 at 9.26.49 AM.png](/images/testing-your-first-application/installing-cypress-and-writing-your-first-test/Screen_Shot_2022-06-28_at_9.26.49_AM.png)

In the new terminal, start the dev server:

```bash
npm run dev
```

Then return to the Cypress test runner and re-run the test by clicking the “Run all tests” button at the top.

![Screen Shot 2022-06-28 at 9.29.01 AM.png](/images/testing-your-first-application/installing-cypress-and-writing-your-first-test/Screen_Shot_2022-06-28_at_9.29.01_AM.png)

You can also press `r` for the same result. Once the test re-runs, it should pass.

![Screen Shot 2022-06-28 at 9.30.25 AM.png](/images/testing-your-first-application/installing-cypress-and-writing-your-first-test/Screen_Shot_2022-06-28_at_9.30.25_AM.png)

## Testing the h1 on the home page

Now that we understand the basic makeup of a spec file and a test, let's write the test that verifies the `h1` on the home page contains the correct text.

First we need to tell Cypress which element to grab—in this case, the `h1`.

A page should only have a single `h1` when following SEO best practices, which is true of our application. That means we can grab it with the [.get()](https://docs.cypress.io/api/commands/get) command:

```ts
describe("home page", () => {
  it("the h1 contains the correct text", () => {
    cy.visit("http://localhost:3000")
    cy.get("h1")
  })
})
```

:::tip
Each time you save your spec file, Cypress automatically re-runs every test in it. This lets you iterate quickly and confirm your tests are working.
:::

![Screen Shot 2022-06-28 at 10.07.53 AM.png](/images/testing-your-first-application/installing-cypress-and-writing-your-first-test/Screen_Shot_2022-06-28_at_10.07.53_AM.png)

On the left side of the screen, the Cypress command log shows every step taken during the test.

- Step 1 is `cy.visit()` loading the application's home page.
- Step 2 is `cy.get()` grabbing the `h1`.

Click any step to see exactly what Cypress was doing at that moment. This is called “time travel debugging”—you can go back in time and inspect each step.

If you click step 2, Cypress highlights the `h1` on the right, showing the element it grabbed.

![Screen Shot 2022-06-28 at 10.10.34 AM.png](/images/testing-your-first-application/installing-cypress-and-writing-your-first-test/Screen_Shot_2022-06-28_at_10.10.34_AM.png)

Now that we have the correct element, we can assert that its text is correct:

```ts
describe("home page", () => {
  it("the h1 contains the correct text", () => {
    cy.visit("http://localhost:3000")
    cy.get("h1").contains("Testing Next.js Applications with Cypress")
  })
})
```

Save the file, and the test should still be passing in Cypress.

![Screen Shot 2022-06-28 at 10.12.36 AM.png](/images/testing-your-first-application/installing-cypress-and-writing-your-first-test/Screen_Shot_2022-06-28_at_10.12.36_AM.png)

Congratulations! You just wrote your first Cypress E2E test 🎉

## Aside: Command Chaining

<YouTube id="WHDvViZazWI" title="Real World Testing with Cypress - Command Chaining"/>

Let's take a closer look at this line:

```ts
cy.get("h1").contains("Testing Next.js Applications with Cypress")
```

First, `cy.get()` grabs the `h1` element on our home page.

Then we [chain](https://learn.cypress.io/cypress-fundamentals/command-chaining) the [contains](https://docs.cypress.io/api/commands/contains) command onto it, passing in the text we expect to find inside the `h1`.

Command chaining is calling one command off the result of another. It's incredibly powerful and you'll see it constantly in Cypress tests. We'll use it countless times throughout this course, but we wanted to point it out early.

To learn more, see our dedicated lesson on command chaining [here](https://learn.cypress.io/cypress-fundamentals/command-chaining).

## Aside: Getting elements best practices

Even though we're just getting started, it's important to pick up some best practices along the way.

In our first test, we grab the `h1` by passing the element name directly to Cypress:

```ts
cy.get("h1")
```

This works—our test is passing—but it isn't ideal.

Why?

If you read the [get command API documentation](https://docs.cypress.io/api/commands/get), you'll see it accepts many things: HTML elements, classes like `.button`, IDs like `#button`, and more. At Cypress, we believe you should target elements using things that don't change.

What do we mean?

Classes and IDs are typically used for styling and for targeting elements with JavaScript, and both tend to change over time. If your tests rely on them, your tests will break when they change.

To avoid this, we recommend using `data` attributes on your elements. In this course we'll use `data-test` attributes specifically—attributes that exist solely for testing. They won't change when your site's design changes, because they aren't tied to class names or IDs.

:::tip
To learn more, read our best practices on the docs site [here](https://docs.cypress.io/guides/references/best-practices#Selecting-Elements).
:::

## Updating our get selector

Now that you've learned about `data` test attributes, let's update our test to use one in practice.

Our test currently looks like this:

```ts
describe("home page", () => {
  it("the h1 contains the correct text", () => {
    cy.visit("http://localhost:3000")
    cy.get("h1").contains("Testing Next.js Applications with Cypress")
  })
})
```

We're going to replace `cy.get("h1")` with a `data-test` selector. If we open Chrome's dev tools and inspect the heading, we'll see this markup:

```html
<h1
  data-test="hero-heading"
  class="mt-4 text-4xl font-extrabold tracking-tight text-white sm:mt-5 sm:text-6xl lg:mt-6 xl:text-6xl"
>
  <span class="block text-gray-900"
    >Testing Next.js Applications with Cypress</span
  >
</h1>
```

The element has `data-test="hero-heading"`, which we can use inside `cy.get()`:

```ts
describe("home page", () => {
  it("the h1 contains the correct text", () => {
    cy.visit("http://localhost:3000")
    cy.get("[data-test='hero-heading']").contains(
      "Testing Next.js Applications with Cypress"
    )
  })
})
```

Save the test, and it should still be passing.

![Screen Shot 2022-06-28 at 2.31.52 PM.png](/images/testing-your-first-application/installing-cypress-and-writing-your-first-test/Screen_Shot_2022-06-28_at_2.31.52_PM.png)

You may be thinking that `cy.get("[data-test='hero-heading']")` is pretty verbose. That's fine for now with a single test, but imagine using `cy.get()` like this across hundreds of tests.

We'll clean this up soon and make `data-test` attributes much easier to work with in the next lesson, when we cover custom Cypress commands. Hold on a little longer!

## Testing the course features

Next, we'll write one more test in this lesson to verify that the features in our homepage hero are correct.

![Screen Shot 2022-06-28 at 2.37.45 PM.png](/images/testing-your-first-application/installing-cypress-and-writing-your-first-test/Screen_Shot_2022-06-28_at_2.37.45_PM.png)

Using dev tools to inspect one of these features reveals the following markup:

```html
<div class="relative mb-6">
  <dt>
    <svg
      xmlns="http://www.w3.org/2000/svg"
      fill="none"
      viewBox="0 0 24 24"
      stroke-width="2"
      stroke="currentColor"
      aria-hidden="true"
      class="absolute w-6 h-6 text-blue-500"
    >
      <path
        stroke-linecap="round"
        stroke-linejoin="round"
        d="M5 13l4 4L19 7"
      ></path>
    </svg>
    <p class="text-lg font-medium leading-6 text-gray-500 ml-9">4 Courses</p>
  </dt>
  <dd class="mt-2 text-base text-gray-500 ml-9"></dd>
</div>
```

There aren't any `data-test` attributes—or anything else specific—we can pass to `cy.get()` to target these elements. We always recommend using `data` attributes when you can, but sometimes you don't control the underlying markup.

For example, when you use a third-party component library, you usually can't modify its HTML to add custom attributes. So what do you do then?

What's the best way to grab these elements?

Let's create a new test just below our first one.

```ts
it("the features on the homepage are correct", () => {
  cy.visit("http://localhost:3000")
})
```

:::info
Notice that each test has to tell Cypress where to navigate in our application.
:::

Run the test and make sure everything passes.

![Screen Shot 2022-06-28 at 2.49.56 PM.png](/images/testing-your-first-application/installing-cypress-and-writing-your-first-test/Screen_Shot_2022-06-28_at_2.49.56_PM.png)

## Using only to run a single test

Now that we have two tests, Cypress re-runs both every time we save. That's fine with a couple of tests, but what about a spec file with dozens of them? Each save would mean waiting for every test to finish, even though we usually only care about the one we're currently writing.

We can tell Cypress to run a single test using `it.only()`:

```ts
describe("home page", () => {
  it("the h1 contains the correct text", () => {
    cy.visit("http://localhost:3000")
    cy.get("[data-test='hero-heading']").contains(
      "Testing Next.js Applications with Cypress"
    )
  })

  it.only("the features on the homepage are correct", () => {
    cy.visit("http://localhost:3000")
  })
})
```

Adding `.only()` after `it` tells Cypress to run only that test.

:::tip
You can add `.only()` to multiple tests, and Cypress will run only those.
:::

![Screen Shot 2022-06-28 at 2.52.22 PM.png](/images/testing-your-first-application/installing-cypress-and-writing-your-first-test/Screen_Shot_2022-06-28_at_2.52.22_PM.png)

As you can see, only the features test runs now.

Next, let's use `cy.get()` to grab the `<dt>` element and see what happens.

```ts
it.only("the features on the homepage are correct", () => {
  cy.visit("http://localhost:3000")
  cy.get("dt")
})
```

![Screen Shot 2022-06-28 at 2.54.16 PM.png](/images/testing-your-first-application/installing-cypress-and-writing-your-first-test/Screen_Shot_2022-06-28_at_2.54.16_PM.png)

Look closely at the get in the Cypress command log and you'll see a small “3” next to it. That means Cypress grabbed three elements, because there are three `<dt>` elements on the page.

You can see this in more detail by clicking the step and opening the dev tools:

![Screen Shot 2022-06-28 at 2.56.26 PM.png](/images/testing-your-first-application/installing-cypress-and-writing-your-first-test/Screen_Shot_2022-06-28_at_2.56.26_PM.png)

A list of three `<dt>` elements was returned.

So how do we grab just one of them? Specifically, we want the first one and we want to confirm its text says “4 Courses.”

Since we're getting back a list, we can use the [eq](https://docs.cypress.io/api/commands/eq) command to access a specific index in that list—exactly what we need.

:::info
Remember that indexes start at 0, so the first element is at index 0, **not** 1.
:::

Update your test:

```ts
it.only("the features on the homepage are correct", () => {
  cy.visit("http://localhost:3000")
  cy.get("dt").eq(0)
})
```

![Screen Shot 2022-06-28 at 3.00.52 PM.png](/images/testing-your-first-application/installing-cypress-and-writing-your-first-test/Screen_Shot_2022-06-28_at_3.00.52_PM.png)

We're now grabbing the first `<dt>` element, which is exactly what we want. Now we just need an assertion that it contains the correct text.

```ts
it.only("the features on the homepage are correct", () => {
  cy.visit("http://localhost:3000")
  cy.get("dt").eq(0).contains("4 courses")
})
```

![Screen Shot 2022-06-28 at 3.02.21 PM.png](/images/testing-your-first-application/installing-cypress-and-writing-your-first-test/Screen_Shot_2022-06-28_at_3.02.21_PM.png)

We're getting an error. Why?

We did this on purpose to highlight that `contains()` is case-sensitive. Our test has:

```ts
.contains("4 courses")
```

But the site says “4 Courses,” with a capital C.

Fix it to get things passing again:

```ts
it.only("the features on the homepage are correct", () => {
  cy.visit("http://localhost:3000")
  cy.get("dt").eq(0).contains("4 Courses")
})
```

![Screen Shot 2022-06-28 at 3.04.03 PM.png](/images/testing-your-first-application/installing-cypress-and-writing-your-first-test/Screen_Shot_2022-06-28_at_3.04.03_PM.png)

:::tip
You can also pass a regular expression to `contains()`. To make a case-insensitive comparison, use:

```ts
cy.get("dt").eq(0).contains(/4 courses/i)
```
:::

Here's our entire spec file so far:

```ts
describe("home page", () => {
  it("the h1 contains the correct text", () => {
    cy.visit("http://localhost:3000")
    cy.get("[data-test='hero-heading']").contains(
      "Testing Next.js Applications with Cypress"
    )
  })

  it.only("the features on the homepage are correct", () => {
    cy.visit("http://localhost:3000")
    cy.get("dt").eq(0).contains("4 Courses")
  })
})
```

## Practice

We still need assertions for the remaining two features—we'll leave those to you for practice.

Pay close attention to this line:

```ts
cy.get("dt").eq(0).contains("4 Courses")
```

You can copy and paste it with minor tweaks to test the other features. Reference the correct index in the list and update the expected text.

If you get stuck, the answer is below.

## Practice Answers

<Disclosure>

```ts
describe("home page", () => {
  it("the h1 contains the correct text", () => {
    cy.visit("http://localhost:3000")
    cy.get("[data-test='hero-heading']").contains(
      "Testing Next.js Applications with Cypress"
    )
  })

  it("the features on the homepage are correct", () => {
    cy.visit("http://localhost:3000")
    cy.get("dt").eq(0).contains("4 Courses")
    cy.get("dt").eq(1).contains("25+ Lessons")
    cy.get("dt").eq(2).contains("Free and Open Source")
  })
})
```

</Disclosure>

## beforeEach hook and refactoring our tests

We've covered a lot in this lesson and we're almost done. Before we finish, let's introduce hooks. Both tests in our spec file use the same line:

```ts
cy.visit("http://localhost:3000")
```

This isn't a problem with only two tests, but again—what happens when the file has dozens?

We can move that duplicated `cy.visit()` into a single [beforeEach() hook](https://mochajs.org/#hooks), a function that runs before each test, which is exactly what we want.

Update your test to look like this:

```ts
describe("home page", () => {
  beforeEach(() => {
    cy.visit("http://localhost:3000")
  })

  it("the h1 contains the correct text", () => {
    cy.get("[data-test='hero-heading']").contains(
      "Testing Next.js Applications with Cypress"
    )
  })

  it("the features on the homepage are correct", () => {
    cy.get("dt").eq(0).contains("4 Courses")
    cy.get("dt").eq(1).contains("25+ Lessons")
    cy.get("dt").eq(2).contains("Free and Open Source")
  })
})
```

We removed `cy.visit()` from each test and placed it inside `beforeEach()` instead. Save the file and run the tests—they should still pass.

![Screen Shot 2022-06-28 at 3.14.57 PM.png](/images/testing-your-first-application/installing-cypress-and-writing-your-first-test/Screen_Shot_2022-06-28_at_3.14.57_PM.png)

## Wrap up

In this lesson you installed Cypress and configured it for E2E testing. You created a spec file, wrote your first tests, and picked up some best practices for getting elements. Finally, you refactored your tests with a `beforeEach` hook to remove duplication.

In the next lesson, you'll learn how to test forms and how to create and use custom Cypress commands.
